# screener-ui

React frontend for the screener app. Part of the screw-u-ats monorepo.

```bash
npm install
npm run dev
```

Expects the screener-api running on `http://localhost:4001` (configurable via `VITE_API_URL`).
