name: Deploy Screener

on:
  push:
    branches: [main]
    paths:
      - 'screener-api/**'
      - 'src/**'
      - 'knowledge/**'

jobs:
  deploy-api:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
